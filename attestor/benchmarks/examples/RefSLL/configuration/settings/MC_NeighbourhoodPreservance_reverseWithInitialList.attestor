# Benchmark: M-N
--description "same list after reversal (expected to fail)"
--classpath "examples/RefSLL/configuration/code"
--class "ListNodeAlt"
--method "reverseList"
--predefined-grammar "SLList"
--rename ListNode=ListNodeAlt next=nextAlt
--initial "examples/RefSLL/configuration/inputs/sll.json"
--model-checking "FG ! {identicNeighbours}"
--admissible-abstraction
--canonical
